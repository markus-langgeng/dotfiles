#!/bin/sh
# display bookmarks with dmenu

bmlist=$(sed -E '/(^\s*$|^#)/ d' $HOME/.local/share/bookmarks)
choice=$(echo "$bmlist" | dmenu -l 10 -p "Open bookmark:")

[ ! -z "$choice" ] && ${1:-$BROWSER} $(echo $choice | awk '{print $2}')
