#!/bin/sh

i=0
files=("$1")
for f in "${files[@]}"; do
    # fext="${f##*.}"
    # [ "$2" = "-np" ] && mv "$f" $(printf "%04d.$fext" "$i")
    # echo mv "$f" $(printf "%04d.$fext" "$i")
    # i=$(( i + 1 ))
    # echo $i
    echo "$f"
done
