#!/usr/bin/env sh

# dwl -s "~/.config/autostart.sh"

# dbus-run-session | dwl -s "$HOME/.config/autostart.sh"
dbus-run-session slstatus -s | dwl -s "$HOME/.config/autostart.sh"
# dbus-run-session slstatus -s | dwl -s "$HOME/.config/autostart.sh" -d 2> ~/.cache/dwl-log.txt
